# Fresh Agent O Human Handoff

- Title: `# Fresh Agent O Handoff: <feature or refactor>`
- Open with an explicit request to use `$gam-orchestration` so the pasted
  packet establishes the Agent O role in the fresh root chat.
- In `Context`, reference the Accepted Requirement Specifications and related
  accepted ADRs or diagrams that Agent O must validate.
- In `Current Status`, state that planning is ready for orchestration.
- Include non-blocking residual questions in `Open Questions`.
- Render only after `$gam-planning`'s readiness criteria pass.
