interface:
  display_name: "GAM Orchestration"
  short_description: "Orchestrate and resume GAM role agents"
  default_prompt: "Use $gam-orchestration to coordinate Agent T, Agent D, and Agent R from accepted planning and resume the same workflow after developer-resolved escalations."
policy:
  allow_implicit_invocation: false
