# Machine-only AWS credentials are supplied by the external deployment
# process.  This file never contains a recovery private key or human identity.
[default]
aws_access_key_id={{ lookup('env', 'GAM_BACKUP_ACCESS_KEY_ID') }}
aws_secret_access_key={{ lookup('env', 'GAM_BACKUP_SECRET_ACCESS_KEY') }}
