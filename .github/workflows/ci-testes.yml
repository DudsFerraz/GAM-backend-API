name: Testes e Cobertura Java

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    # 1. Adicionamos o serviço do PostgreSQL
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_DB: gam_db
          POSTGRES_USER: usuario_teste
          POSTGRES_PASSWORD: senha_teste
        # Mapeia a porta padrão do container para a máquina do GitHub
        ports:
          - 5432:5432
        # Aguarda o banco estar pronto antes de rodar os testes
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Configurar Python para contratos operacionais
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: operations/ansible/requirements-test.txt

      - name: Instalar dependencias dos contratos operacionais
        run: python -m pip install --requirement operations/ansible/requirements-test.txt

      - name: Instalar colecoes Ansible fixadas para os cenarios operacionais
        run: ansible-galaxy collection install --requirements-file operations/ansible/collections/requirements.yml

      - name: Executar contratos operacionais de producao
        run: python operations/ansible/test_production_backup_aws_contracts.py

      - name: Executar cenarios do provedor Better Stack com Ansible
        run: python operations/ansible/test_better_stack_provider_scenarios.py

      # 2. Executamos os testes injetando as variáveis de ambiente
      #dasjhdsaid
      - name: Executar testes com Maven
        env:
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/gam_db
          SPRING_DATASOURCE_USERNAME: usuario_teste
          SPRING_DATASOURCE_PASSWORD: senha_teste
          SPRING_PROFILES_ACTIVE: default
          JWT_SECRET_KEY: 7be123441ce4c2c2c04a9e2e27d802ce81be1708eeac649b747e93907b558c2d38009c09
          CORS_ALLOWED_ORIGINS: http://localhost:5173
        run: mvn clean test

      - name: Fazer Upload do Relatório de Cobertura
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: relatorio-cobertura-jacoco
          path: target/site/jacoco/
